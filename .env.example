# Laravel origin only (no /api suffix). Set in .env.local before using auth.
# Public configuration bundled at build time; never put secrets here.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Empty for root hosting. GitHub Actions sets this to the repository path for Pages.
NEXT_PUBLIC_BASE_PATH=
